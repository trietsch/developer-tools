build-and-test:
  stage: build-and-test
  image: adoptopenjdk/openjdk14:debian-slim
#  variables:
#    MAVEN_PROFILE: "banaantje"
  script:
    - |
      if [[ -z "${MAVEN_PROFILE}" ]]; then
        echo "No profile"
      else
        echo "Profile = ${MAVEN_PROFILE}"
      fi
